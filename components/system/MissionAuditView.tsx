"use client";

// ── ③ 系统维护 · Mission 审计（P21-T5-4B）───────────────────────────────────
// 承接原 /admin/mission-audit 内部页 —— 它此前无任何导航入口，且 T5-1 前
// 因两个前缀都不在而回落到老板工作区。本文件是薄封装，不复制逻辑。
export { default } from "@/app/admin/mission-audit/MissionAuditPanel";
